version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  runtime-version: 3.9
  command: streamlit run --server.port 8080 --server.address 0.0.0.0 app.py
  network:
    port: 8080
